#include<bits/stdc++.h>
#define rep(i,a,n) for(auto i = a ; i <= (n) ; i++)
using namespace std;
using ll = long long int;
void solve(){
}
int main(){
    int T;cin >> T;
    while(T--)solve();
    return 0;
}